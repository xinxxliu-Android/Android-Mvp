<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="app_dialog" parent="android:style/Theme.Dialog">
        <item name="android:background">#00000000</item>share_dialog
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <style name="AppUpDateDialog" parent="android:style/Theme.Dialog">
        <!-- 无边框设置 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否半透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 是否没有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 背景 -->
        <item name="android:windowBackground">@mipmap/global_window_bg</item>
        <!-- 是否背景模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!--dialog遮罩透明度-->
        <item name="android:backgroundDimAmount">0.3</item>
    </style>

    <!-- 无人机设置界面Fragment  SeekBar按钮样式 -->
    <style name="DroneFragmentSeekBarStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:maxHeight">@dimen/dp_15</item>
        <item name="android:progressDrawable">@drawable/shape_module_settings_activity_seek_bar_bg
        </item>
        <item name="android:thumb">@drawable/module_setting_activity_seek_bar_round</item>
    </style>
    <!--    云台字体样式-->
    <style name="gimbalTextStyle">
        <item name="android:layout_weight">1</item>
        <item name="android:textSize">@dimen/sp_15</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">@dimen/dp_0</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <!--    飞行字体样式-->
    <style name="flyTextStyle">
        <item name="android:textSize">@dimen/sp_15</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <!--导入杆塔 复选框颜色 -->
    <style name="import_tower_checkbox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlActivated">@color/color_3a9eff</item>
    </style>
</resources>